variables:
  VERSION: "1.1-SNAPSHOT"
  DOCKER_IMAGE_NAME: "docker/images/sphinx-doc"

stages:
  - package
  - secure

Deploy Docker image:
  stage: package
  image: docker-private.gesis.intra/gesis/dc:5.10
  services:
    - name:  docker-private.gesis.intra/gesis/dind:5.6
      alias: docker
  script:
    - docker-ci deploy-image

Scan Docker image for vulnerabilities:
  stage: secure
  image: docker-private.gesis.intra/gesis/dc:5.10
  services:
    - name:  docker-private.gesis.intra/gesis/dind:5.6
      alias: docker
  script:
    - docker-ci scan-image
  artifacts:
    paths:
      - grype-report.html

Generate bill of materials of Docker image:
  stage: secure
  image: docker-private.gesis.intra/gesis/dc:5.10
  services:
    - name:  docker-private.gesis.intra/gesis/dind:5.6
      alias: docker
  script:
    - docker-ci generate-bom
  artifacts:
    paths:
      - syft-report.txt
